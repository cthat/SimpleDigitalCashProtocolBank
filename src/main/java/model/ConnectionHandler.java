package model;

public class ConnectionHandler implements Runnable {
    public void run() {
        /*
        Rebuild list of orders
        sign the last order if it matches the amount of orders seen + 1?
        send the signed value back for decalculation at client
         */

    }

    //TODO handles the java connection that comes in with the order list
}
